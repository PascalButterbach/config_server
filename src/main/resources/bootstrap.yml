spring:
  profiles:
    active: composite
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        composite:
        -
          type: git
          uri: https://github.com/PascalButterbach/config_server
          clone-on-start: true
          search-paths: /configs
          default-label: master

        bootstrap: true